{
  "name": "fare-radar",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:ts": "tsc -p tsconfig.json --noEmit",
    "test": "pytest -q",
    "check": "npm run check:ts && pytest -q"
  }
}
