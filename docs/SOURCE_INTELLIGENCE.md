# Source Intelligence

`SourceRegistry` describes where intelligence is obtained. `ProviderAccessRegistry`, `PromotionChannelRegistry`, and `AgencyPartnerRegistry` remain separate contracts. Popularity can change acquisition priority, never `VerificationAuthority`.
